# Ensure CI install behavior matches local
frozen-lockfile=true
# Ignore building large prisma engines on Vercel/CI
ignored-built-dependencies=@prisma/client @prisma/engines prisma
# Only build native deps we truly need
only-built-dependencies=sharp
